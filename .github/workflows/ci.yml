name: CI
on:
  push:

permissions:
  contents: read # to fetch code (actions/checkout)

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
    - uses: actions/checkout@v4
    - uses: sbt/setup-sbt@v1
    - uses: actions/setup-java@v4
      with:
        java-version: 24
        distribution: zulu
    - run: sbt "show name"
    - run: sbt "show name"
    - run: sbt "show name"
    - run: sbt "show name"
    - run: sbt "show name"

    - run: sbt -J-XXX:AOTMode=record -J-XXX:AOTConfiguration=app.aotconf "show name"
    - run: sbt -J-XXX:AOTMode=create -J-XXX:AOTConfiguration=app.aotconf -J-XXX:AOTCache=app.aot "show name"

    - run: sbt -J-XX:AOTCache=app.aot "show name"
    - run: sbt -J-XX:AOTCache=app.aot "show name"
    - run: sbt -J-XX:AOTCache=app.aot "show name"
    - run: sbt -J-XX:AOTCache=app.aot "show name"
    - run: sbt -J-XX:AOTCache=app.aot "show name"
